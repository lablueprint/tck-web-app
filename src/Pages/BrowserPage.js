import React from 'react';
import { BookBrowser } from '../Components';

function BrowserPage() {
  return (
    <BookBrowser />
  );
}

export default BrowserPage;

/** NOTES
 * This page may seem unnecessary for now, but will provide an clearer way
 * to combine with other components like Collections later down the line.
 */
